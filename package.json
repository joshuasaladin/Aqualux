{
  "name": "aqualux-crm",
  "version": "1.0.0",
  "description": "Aqualux Concierge CRM — track client service requests, follow-ups, and payments",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "DEMO=1 node src/server.js",
    "seed": "DEMO=1 node -e \"import('./src/seed.js').then(m => m.seedDemo(true))\""
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
